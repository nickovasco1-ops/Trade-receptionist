# Environment — never commit real secrets
.env
.env.local
.env.*.local
.env.test

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
playwright-report/
test-results/
e2e/.auth/
.gitnexus
.claude/worktrees/
.stitch/
DESIGN.md
.vercel

# Health reports are generated per run and published as CI artifacts + job
# summaries. Committing one per day would be noise; the bundle baseline in
# .claude/health/ IS tracked, because the size check compares against it.
reports/
